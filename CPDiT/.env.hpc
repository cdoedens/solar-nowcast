# HPC Environment Configuration for CPDiT
# This file documents the HPC environment setup

# Module Configuration
HPC_MODULE_PATH="/g/data/dk92/apps/Modules/modulefiles/"
HPC_MODULE_NAME="pet/0.4.0"

# Project Paths
PROJECT_ROOT="/home/548/cd3022/repos/solar-nowcast/CPDiT"
SCRIPTS_DIR="${PROJECT_ROOT}/scripts"
CONFIGS_DIR="${PROJECT_ROOT}/configs"
OUTPUTS_DIR="${PROJECT_ROOT}/outputs"

# Do NOT use local venv when using HPC environment
# Remove or deactivate if active: deactivate

# Usage:
# export $(cat .env.hpc | grep -v '^#' | xargs)
# source hpc_setup.sh
