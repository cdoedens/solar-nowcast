model:
    name: base
    forecast_lead: 3
    parameters:
        random_state: 42
        n_estimators: 2000
        early_stopping_rounds: 50
        learning_rate: 0.05
        eval_metric: rmse
data:
    predictors:
        - csi
        - delta_csi
    target: csi
